clusterName: teleport.noa.re
service:
  type: NodePort
ingress:
  useExisting: true
highAvailability:
  requireAntiAffinity: true
  certManager:
    enabled: true
    issuerName: letsencrypt
    issuerKind: ClusterIssuer
persistnce:
  volumeSize: 1GiB
